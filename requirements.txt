fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.49.0
uvloop==0.22.1
watchfiles==1.2.0
click==8.4.2
annotated-doc==0.0.4

# Pydantic v2 + Settings + dataclasses-style validation
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.2
annotated-types==0.7.0
typing-inspection==0.4.2

# Async HTTP client (FastAPI test client + outbound calls)
httpx==0.28.1
h11==0.16.0
httpcore==1.0.9
anyio==4.14.1
idna==3.18
sniffio==1.3.1
certifi==2026.6.17

# LLM provider SDKs (raw calls + Pydantic-validated tool calling)
openai==2.44.0
anthropic==0.115.1
jiter==0.16.0
tqdm==4.68.3

# Orchestration (LangGraph + checkpointing + langchain-core)
langgraph==1.2.7
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain-core==1.4.8
langchain-protocol==0.0.18

# Persistence (Postgres + pgvector, sync + async drivers)
sqlalchemy==2.0.51
asyncpg==0.31.0
psycopg[binary,pool]==3.3.4
psycopg-binary==3.3.4
greenlet==3.5.3
pgvector==0.4.2
numpy==2.5.0

# Broker + cache
redis==8.0.1

# Logging + observability
loguru==0.7.3

# HTTP utility (Langfuse + outbound webhooks)
requests==2.34.2
requests-toolbelt==1.0.0

# Retry / resilience
tenacity==9.1.4

# Env loading
python-dotenv==1.2.2

# ---------------------------------------------------------------------------
# Test toolchain (mirrored from [project.optional-dependencies] dev).
# ---------------------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
