"""Pytest test package for the agentic AI platform.

Adding this ``__init__`` makes test files cleanly discoverable as
sub-modules of ``tests`` (so relative imports work) without leaking
behaviour into the project's release artefacts.
"""
